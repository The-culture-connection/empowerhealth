/**
 * Authentication Provider
 * Re-exports AuthProvider from contexts for consistency
 */

export { AuthProvider, useAuth } from '../contexts/AuthContext';
export type { UserRole } from '../contexts/AuthContext';
